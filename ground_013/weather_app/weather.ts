// main api call function
async function main(): Promise<number> {
  // pnpm run weather LOCATION
  
  return await Promise.resolve(0);
}

// call our main function when weather.ts file runs
main().catch((err) => console.error(err));