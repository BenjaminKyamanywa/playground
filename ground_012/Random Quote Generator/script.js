// Get quotes from API
const getQuotes = async = async () => {
  // add URL for our API call
  const apiURL = 'https://type.fit/api/quotes';

  try {
    const response = await fetch(apiURL);

  } catch (error) {
    // Catch error 
  }
}